# External integrations / API

- **OpenAI API** — генерация ответов и эмбеддинги (`openai`, LangChain). Ключ через env.
- **Telegram Bot API** — боты на `python-telegram-bot`.
- **FastAPI-эндпоинты** (учебные), напр. `lesson_11`: `GET /`, `POST /api/get_answer` (вопрос → ответ по базе через FAISS-retrieval).
- **Локальные/контурные модели** — `lesson24` (ноутбук про запуск моделей локально).
